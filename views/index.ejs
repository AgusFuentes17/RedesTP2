<%- include('template/cabecera', {tituloweb: 'Inicio EJS'}); %>

<head>
    <meta charset="utf-8">

    <style>
      body, html {
          margin: auto;
          padding: 0;
          overflow: hidden;
      }
      #nameFormContainer {
          position: absolute;
          width: 100%;
          height: 100%;
          background-color: rgba(0, 0, 0, 0.8);
          display: flex;
          justify-content: center;
          align-items: center;
      }

      #nameForm {
          background-color: white;
          padding: 20px;
          border-radius: 10px;
          text-align: center;
      }

      #nameInput {
          padding: 10px;
          font-size: 16px;
          width: 200px;
          margin-bottom: 10px;
      }

      #startGameButton {
          padding: 10px 20px;
          font-size: 16px;
          cursor: pointer;
      }
      #playerNameLabel {
        position: absolute;
        color: white;
        font-size: 20px;
        font-weight: bold;
        text-shadow: 1px 1px 2px black;
        transform: translate(-50%, -50%); /* Para centrar el texto en su posición */
    }
    #controles{
        position: absolute;
            top: 60px;
            left: 70px;
            background-color: rgba(0, 0, 0, 0.7); /* Fondo semitransparente */
            color: white;
            padding: 10px;
            border-radius: 5px;
            font-size: 13px;
            z-index: 100;
    }
    </style>
</head>
<body>
    <div id="controles">
    <p>
     <p>CONTROLES</p>
     <p>W: adelante</p>
     <p>A: izquierda</p>
     <p>S: Atras</p>
     <p>D: derecha</p>
     <p>Flechas:(Izquierda, derecha) Mover camara</p>
     <p>F: Detener Jugador</p>
     <p>Click izquierdo: balas</p> 
    </div>
    <script type="module" src="/js/script.js"></script>
    <script src="/socket.io/socket.io.js"></script>
</body>

<%- include('template/footer'); %>